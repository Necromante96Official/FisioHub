{
  "name": "fisiohub",
  "private": true,
  "version": "0.5.6",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 4173",
    "preview": "vite preview --host 127.0.0.1 --port 4173",
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "check": "npm run typecheck && npm run build",
    "test": "npm run build && node --test tests/*.test.mjs zenfisio-ColetorDeDados/tests/*.test.mjs",
    "check:extension": "npm --prefix zenfisio-NotificarMensagens run typecheck && npm --prefix zenfisio-NotificarMensagens run test"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^8.0.16"
  }
}
